# 全站 UI 五轮审计与修复

日期：2026-09-05。技能：Impeccable audit + polish。范围：Web，保留现有黑白灰、身份轨与连续阅读流，以及手机日期左、筛选右的布局。

## 实现一致性结论

通过：主要页面沿用共同身份轨、细线阅读列表、主题与移动导航，没有引入新视觉体系。此次确认并修复 4 项问题（P1：1，P2：3；P0：0）。未为凑满轮次强行增加样式改动。

| 维度 | 审计判断 / 4 | 依据与边界 |
| --- | --- | --- |
| 无障碍 | 3 | 检查命名、主题对比度、焦点返回和减少动态效果；自动扫描不能替代完整读屏测试 |
| 性能 | 3 | 保留屏外内容跳过绘制与既有动效清理，消除延迟颜色过渡；没有进行性能基准测试 |
| 响应式 | 3 | 320/390px、844×390 横屏、901px 断点及 1440px 桌面检查，无页面水平溢出 |
| 主题 | 3 | 修复深色建议列表边框与屏外标题切换主题后的短暂低对比度 |
| 实现一致性 | 3 | 修改限于现有 CSS；检测器保留两项已有设计尺度建议 |
| 合计 | 15 / 20 | Good；保守定性评分，不构成完整 WCAG 合规认证 |

## 第一轮：整体布局、阅读层级

- 浏览首页、每日动态、每日关注、设计收藏、抖音收藏、开源关注、构建、问一问，检查窄屏布局与可操作元素。
- **P2 / 一致性：** Ask 建议按钮继承基础 Button 的四边 1px 边框；深色规则把透明边框全部染成灰色，产生方框列表。位置：`components/ask-chat.module.css` 的 suggestions 按钮规则。
- 修复：先清除基础边框，只留下列表底部分隔线。对应 Impeccable polish。
- 验证：真实页面计算边框上/右/下/左分别为 0/0/1/0px，320px 无横向溢出。

## 第二轮：触控与响应式

- **P2 / 响应式：** 设计收藏标题是独立详情入口，原单行链接只有约 16.5px 高。位置：`app/globals.css` 的 design-curation 标题链接。
- **P2 / 响应式：** 构建卷宗标题独立链接约 26px 高。位置：`components/works.module.css` 的 dossierIntro。
- 修复：移动断点内提供 44px 最小高度，文字垂直居中，不改变字号。对应 Impeccable adapt / polish。
- 验证：320px 下两类链接实际高度均为 44px。901px 与 844×390 下检查动态、构建、Ask，没有页面水平溢出；横屏 Ask 输入框仍在视口内。

## 第三轮：详情与媒体

- 检查每日动态、每日关注、设计收藏、开源关注、构建各一条真实详情；均无页面横向溢出，返回入口至少 44px。
- 开源详情代码块在自己的滚动区域显示长行，是正确行为，不按页面溢出修复。
- 构建项目索引能够定位章节，标题可读；大图正常打开、下一张计数由 1/4 更新为 2/4，关闭后解除滚动锁并将焦点返回原缩略图。
- 未发现新的需修复问题，此轮保留现有实现。

## 第四轮：主题、无障碍与动效

- 对八个主要页面执行浅色/深色 Axe WCAG A/AA 自动扫描，结合计算样式复核。主题过渡中的暂态结果与最终状态分开处理。
- **P1 / 主题与无障碍：** 长列表的屏外标题颜色过渡会延迟到布局/显示时启动。切换主题后，屏外旧颜色与新背景短暂组合，测得暗底对比度 1.34:1、亮底 1.09:1。位置：`app/globals.css` 的策展与动态标题、`components/open-source.module.css` 的条目标题。
- 修复：删除这三类标题的颜色 transition，让主题与悬停颜色即时更新，保留其他导航、页面和媒体动效。对应 Impeccable harden / polish。
- 修复后的主要页面主题扫描不再报告上述标题问题。设计收藏一个屏外分类文本被扫描器误判为叠在媒体深色底上；将该条目实际滚入视口复核后，局部 color-contrast 检查通过，未据此修改正确配色。

## 第五轮：整体回归与收尾

- 重新检查八个主要页面的 1440px 桌面布局，并查看首页、动态、Ask 实际截图；复查移动端布局、筛选菜单和日期吸顶。
- 手机筛选菜单正常展开，共 7 个单选项，当前项可选取关闭。日期栏顶部 53.59px、导航底部 53.12px，保持紧邻，无页面水平溢出。
- 减少动态效果模式：六条信号轨动画均为 none，页面未残留过渡覆盖层。
- 最终交互期间通过 CDP 收集的浏览器异常/console error 数组为空。
- Impeccable 机械检测器运行一次：仅两条 advisory，均为之前手机首页的 10px 头像圆角、18px 姓名字号与 DESIGN.md 尺度表差异。本次未引入这些值，不以审计为由改变用户已确认的首页外观。
- `pnpm typecheck`、`pnpm lint`、`pnpm test`、`pnpm build` 全部通过：Vitest 29 个文件 / 103 项，Node test 81 项，合计 184 项测试。

## 验证边界

- 浏览器验证使用 ego lite 和真实开发页面。检查了代表性详情与交互，并非穷举每条内容、每种设备或完整读屏测试。
- 未向真实模型发送测试问题，未执行数据库写入或数据同步；Ask 服务端回复能力不包含在本次结论中。
- 未提交或推送代码；保留本次开始前已有的工作区修改。五轮已结束，不继续追加美化轮次。
