> 后续决策（2026-09-05）：项目仅保留 Web，原生应用及其构建已删除。本文保留为历史记录，原生端建议不再适用。

# UI 修复目标与验证记录

日期：2026-09-05。来源：[全项目 Impeccable 审视](critique/2026-09-04T22-48-55Z__app.md)。

## 目标与停止条件

最多执行三轮“修复 → 验证”，修复五项优先问题：设计收藏标题网格污染、移动当前导航屏外、问答停止丢失已有回答、会话草稿与消息恢复、Android 正文链接。补齐相同问答路径的中文选字回车保护和错误内容保留。保持现有身份轨、连续流、单色、字号和首访仪式。全部完成即可提前结束；第三轮后不再启动第四轮。代码保持未提交、未推送。

结果：**三轮完成，目标内问题全部修复。**

## 第一轮：修复与基础验证

- 将普通策展整行链接的网格限定到直接子链接，设计标题与媒体链接不再被污染。
- 移动导航只滚动自身容器，并监听尺寸变化。修正审视中的原因判断：原本已有挂载时 scrollIntoView；缺陷是窗口缩窄后不重新定位，并可能连带滚动其他容器。
- 问答停止与错误用独立状态提示，保留正文和引用；错误可将原问题与范围填回输入框，由用户再次发送。
- sessionStorage 保存当前会话的草稿、检索范围和消息；返回时恢复。未完成的流在快照中落为“已停止”，不会假装后台继续生成。清空同步删除记录。存储损坏、禁用或满额不阻断当前页面输入。
- Enter 提交增加 composition/keyCode 229 保护，Shift+Enter 保持换行。
- Android Markdown 使用 Compose LinkAnnotation.Url 保留目标与点击/辅助功能语义。
- 六项问答组件测试通过；Android 32 项单元测试、assembleDebug 通过，其中新增 3 项链接语义回归测试。
- 全量 Web 测试：Vitest 103 项、Node test 81 项通过；lint、build 通过。首次 typecheck 被旧 .next/types 中已移除路由阻塞，next typegen 重新生成后通过。
- 浏览器实证：设计标题为 inline、padding 0；390px 当前导航位于 x338.5–374.02，页面无横向溢出。草稿与范围跨页恢复，模拟 SSE 停止后正文与引用保留，返回继续可读，清空后存储为空。

## 第二轮：交互和回归复核

- 普通策展行仍保持双列网格（桌面 136px/602px），无页面横向溢出。
- 本地 SSE 模拟错误时保留部分正文、显示错误提示与“重新提问”；点击只填回原问题并聚焦，未自动新增请求。
- 检查移动、深色与 reduced-motion 状态。
- 收尾发现：首访设计列表由 Motion 写入的初始样式与揭幕清理存在水合不一致；新中断提示在深色下沿用浅色 quiet，需提高对比度。进入第三轮集中修复。

## 第三轮：最后修复与确认

- 首屏策展条目不再设置 Motion animate 目标，SSR 和客户端均静态输出；仅追加条目播放既有动效。
- 深色中断提示使用 #999，重试按钮继承文字颜色，移动触达高度设为 44px。
- 实际首访 /design：浏览器错误数组为空、Next issue badge 不存在、标题 inline、首行无额外初始样式。
- 390px 深色 /ask：提示色 rgb(153,153,153)，底色 rgb(24,24,24)，对比度 **6.23:1**；重试按钮 **44px**；当前导航位于可视区；无页面横向溢出，浏览器错误为空。已查看最终渲染截图。
- 再次完成 pnpm typecheck、pnpm lint、pnpm test、pnpm build，全部通过。Android 第一轮后无代码变更，不重复构建。

## 验证边界与清理

- 问答使用浏览器本地模拟 SSE，没有向外部模型发送测试问题；实际模型服务的可用性不属于本次验证结论。
- Android 完成编译和链接注解单元测试，未运行设备点击测试；iOS 未修改共享契约，无需同步模型。
- ego lite 验证任务 14、15 均已关闭；本次启动的开发服务已停止。
- 构建生成的 next-env.d.ts 恢复为本轮开始时的内容；保留原有 data/curation.sqlite 工作区变化，未暂存或提交。
- 未执行第四轮。
