---
name: 陈远｜运行中的工程档案
description: 面向 Agent 工程实践的个人身份轨与每日关注信息流。
colors:
  ink: "#1c1c1e"
  surface: "#ffffff"
  muted: "#656568"
  quiet: "#767676"
  summary: "#6f6f72"
  line: "#eeeeee"
  dark-surface: "#181818"
  dark-ink: "#f4f4f4"
  loader-green: "#24cb71"
  loader-red: "#ef4444"
  loader-amber: "#f2c94c"
  paper: "#f8f8f8"
  paper-dark: "#f6f2e9"
typography:
  display:
    fontFamily: '"SF Pro Display", -apple-system, BlinkMacSystemFont, "PingFang SC", "Microsoft YaHei", Arial, sans-serif'
    fontSize: "clamp(2.05rem, 4.2vw, 3.85rem)"
    fontWeight: 610
    lineHeight: 1.04
    letterSpacing: "-0.04em"
  title:
    fontFamily: '"SF Pro Text", "SF Pro Display", -apple-system, BlinkMacSystemFont, "PingFang SC", "Microsoft YaHei", Arial, sans-serif'
    fontSize: "0.95rem"
    fontWeight: 620
    lineHeight: 1.15
    letterSpacing: "-0.035em"
  body:
    fontFamily: '"SF Pro Text", "SF Pro Display", -apple-system, BlinkMacSystemFont, "PingFang SC", "Microsoft YaHei", Arial, sans-serif'
    fontSize: "0.82rem"
    lineHeight: 1.38
  signal:
    fontFamily: "ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace"
    fontSize: "0.62rem"
    fontWeight: 650
  scale:
    caption-xs: "0.67rem"
    caption-sm: "0.72rem"
    caption: "0.74rem"
    caption-lg: "0.76rem"
    body-sm: "0.78rem"
    body-md: "0.88rem"
    mobile-bio: "0.875rem"
    mobile-identity: "1.125rem"
    body-lg: "0.9rem"
    markdown-h3: "1rem"
    markdown-h2: "1.18rem"
    markdown-h1: "1.4rem"
    detail-h1-min: "1.45rem"
    detail-h1-fluid-min: "1.55rem"
    detail-h1-max: "1.85rem"
    detail-h1-fluid-max: "2.15rem"
    stream-h3-min: "0.88rem"
    stream-h3-max: "1rem"
rounded:
  micro: "0.09375rem"
  xs: "0.125rem"
  sm: "0.2rem"
  md: "0.25rem"
  lg: "0.3125rem"
  xl: "0.35rem"
  2xl: "0.4rem"
  3xl: "0.45rem"
  4xl: "0.5rem"
  5xl: "0.7rem"
  machine: "1.4rem"
  avatar: "12px"
  mobile-avatar: "10px"
  media: "12px"
  pill: "999px"
spacing:
  compact: "8px"
  control: "12px"
  mobile-gutter: "16px"
  rail: "30px"
  section: "32px"
  stream-row: "26px"
components:
  external-link:
    backgroundColor: "{colors.ink}"
    textColor: "{colors.surface}"
    rounded: "{rounded.pill}"
    padding: "9px 13px"
  stream-row:
    textColor: "{colors.ink}"
    padding: "24px 0"
  tag:
    textColor: "#6f6f72"
    rounded: "{rounded.pill}"
    padding: "7px 10px"
---

# Design System: 陈远｜运行中的工程档案

## Overview

**Creative North Star: “运行中的工程档案”。** 这是一个以个人工程身份为锚点、以每日判断为主内容的桌面优先站点。页面不把内容包进卡片，也不使用营销式 hero；身份信息作为始终可见的左侧轨道，策展和文章作为右侧连续阅读流。

视觉参考来自 Ample Studio 的克制、轻量和留白节奏，但内容与交互服务于 Agent 工程实践：信号场传达技术广度，逐字叙事传达个人履历，策展流传达持续输入与判断。

**Key Characteristics:**

- 高对比黑白灰为主体；绿色只属于 Loading 动画，不进入常规页面。
- 平面、细分割线、连续流；不要用卡片堆叠制造层级。
- 桌面两栏是主布局；窄屏才收敛为单栏。

## Colors

### Primary

- **Ink** (`#1c1c1e`)：标题、链接、按钮和主要内容。默认内容不引入额外品牌色。
- **Loader Green / Red / Amber** (`#24cb71` / `#ef4444` / `#f2c94c`)：仅用于电池充电的 Loading 状态（红→黄→绿充电序列）；加载完成后不可延续为页面装饰色。

### Neutral

- **Surface** (`#ffffff`)：浅色主题的完整底面。
- **Muted** (`#656568`)：次级信息和正文动效中的低强调文本。白底对比度 ≥4.5:1（WCAG AA），2026-08-15 起按要求加深。
- **Quiet** (`#767676`)：时间、作者、说明性元信息。白底对比度 ≥4.5:1（WCAG AA），2026-08-15 起按要求加深。
- **Summary** (`#6f6f72`)：摘要正文、标签、eyebrow 与 hover 降调文本；介于 Muted 与 Quiet 之间的中间灰。白底对比度 ≥4.5:1（WCAG AA），2026-08-15 起由 `#969699` 加深。
- **Paper** (`#f8f8f8`，深色 `#f6f2e9`)：仅关于我打印稿的纸面。
- **Error** (`#994545` 系列）：表单与数据错误的语义色，仅出错时使用。
- **Shadow/Overlay**：遮罩与打印阴影只用 `rgb(0 0 0 / N%)` 黑色透明度阶。
- **Line** (`#eeeeee`)：流式内容和区域之间的唯一分隔方式。
- **Dark Surface / Dark Ink** (`#181818` / `#f4f4f4`)：深色主题的成对替换；暗色模式同样保持单色体系。

**单色优先规则。** 除 Loading 绿外，不以黄色、渐变或高饱和点缀争夺内容注意力。

## Typography

系统字体以 SF Pro 与中文系统字体为主，保证身份、策展标题与长文在 macOS 中文环境中的一致字形。技术信号场独立使用等宽字体，且只服务于技术语义。

- **Display**（610，`clamp(2.05rem, 4.2vw, 3.85rem)`，1.04）：仅开屏等非内容场景使用，内容页标题一律用 Detail headline 档；最大负字距为 `-0.04em`。
- **Title**（620，`0.95rem`，1.15）：左侧姓名、列表区标题与轻量区域标题。
- **Stream headline**（500，`clamp(0.88rem, 0.95vw, 1rem)`，1.32，`-0.018em`）：信息流条目标题；使用 `text-wrap: pretty`。信息流标题保持克制，层级靠与摘要的粗细/颜色对比，不靠大字号。
- **Body**（`0.78rem`–`0.96rem`，1.32–1.8）：简介、摘要与 Markdown 正文按阅读密度递进；简介正文可撑满身份轨，不再使用固定窄列。
- **Signal**（650，`0.62rem`）：仅技术词节点，不作为普通 UI 正文字体。
- **Detail headline**（610，`clamp(1.55rem, 2.15vw, 2.15rem)`，1.16，`-0.038em`，`text-wrap: balance`）：内容详情页 h1。

**语义字号规则。** 内容页面的标题不使用 display 级别——长标题在超大字号下会失控；display 档仅保留给开屏等非内容场景。首页内容通过粗细、留白和分隔线建立层级，不依赖超大标题。

**字号阶梯。** frontmatter 的 `typography.scale` 是全站允许的字号清单（caption 0.67–0.76 / 正文 0.78–0.96 / Markdown 1–1.4 / 详情与信息流标题档）；新增字号先进清单再进代码，探测器以此为准。

## Layout

**双端阅读规则。** 桌面保留身份轨与连续内容流；手机按阅读顺序重排，而非缩小桌面两栏。断点为 900px，紧凑边距断点为 560px。

- 桌面身份轨最小 28rem、最大 38vw，保持 sticky；内容流最大 50rem，Ask 内容栏最大 42rem。普通列表与详情随文档滚动，Ask 使用内部消息滚动区。
- 手机首页依次呈现身份与外链、栏目导航、完整中文介绍、技术词条。首页头像 72px，词条区高 128px；阅读栏目使用紧凑身份头部。
- 560px 以下带日期栏的内容流采用 16px 左右边距，与身份区、导航对齐。日期栏直接衔接导航，不再增加独立顶部空白。
- 不以改变文字字号扩大触达区域：手机关键独立按钮、标题链接、筛选与返回入口至少 44px 高。长文本自然换行，代码与媒体只在自己的区域滚动。

## Elevation & Depth

本系统是**纯平面**的。层级来自网格、留白、细线与文本对比，不使用卡片阴影、毛玻璃或浮动面板。头像与媒体可使用 `12px` 小圆角承载真实图像，但不能叠加大阴影。

**平面优先规则。** 除媒体缩放、主题按钮 hover 与必要的状态反馈外，不为“看起来高级”添加阴影、描边加阴影组合或装饰性背景。唯一例外是关于我打印稿的打印机机身（见「关于我打印稿」的受控拟物例外）。

## Shapes

媒体与头像使用轻圆角，普通内容行不包容器。手机首页头像沿用 mobile-avatar 圆角；菜单用细边框与小圆角，实心胶囊仅用于适合强调的详情来源出口，不用于身份区外链。

## Components

### 身份轨

- 身份区是全站稳定锚点。桌面呈现头像、姓名、外链、技术信号场与简介；手机首页的介绍在技术词条之前，内容页收敛为紧凑头部。
- 桌面使用 `position: sticky` 与 `100dvh`；详情页必须复用相同身份轨，不能出现第二套侧栏视觉语言。
- GitHub、语雀、关于我构成身份轨的链接行：quiet 文本 + 图标的同行链接，墨色 hover；均为外链或弹层，站内入口不进身份轨。

### 内容导航

- 桌面内容区头部是刊头而非 tab：当前版块为 Title 档刊名（0.95rem/620/`-0.035em`）居左，兄弟版块为 quiet（`#767676`）`0.78rem` 同行链接居右，hover 与过渡中转墨色；整个头部共享唯一一条 `1px` 细线并保持 sticky。桌面无独立「首页」：`/` 的右侧即每日动态，刊头不再提供「首页」链接。
- 刊头承载全部内容入口（每日动态/每日关注/设计收藏/抖音收藏/开源关注/构建/问一问）：问一问虽是对话式动作，仍与阅读版块等权出现在版块序列末位；`/ask` 页的刊名是"问一问"，兄弟位与其余栏目页一致。
- 不叠加两排分类 Tab。每日动态分类放在日期行右侧的单选菜单；开源关注保留带计数的轻量主题文字索引，当前项用短下划线，小屏允许换行。
- 窄屏（≤900px）沿用身份区下方的横排移动导航（含首页链接），允许横滑并保持当前项可见。手机导航 0.78rem，当前项加重；触达区域至少 44px。
- 阅读版块各有独立路径（`/ai-news`、`/curation`、`/design`、`/douyin`、`/open-source`、`/works`），刊头兄弟链接与移动导航都指向完整路径；首页遗留的 `?view=` 仅为旧链接兼容，不再是导航机制。

### 手机头部与日期工具栏

- 阅读列表滚动超过 80px 后，身份头部以 240ms 向上收起，只留下栏目导航；回到顶部 16px 内展开。通过 sticky 停靠偏移保持文档高度，避免反复跳动；首页与 Ask 不应用该收起行为。
- 首页与栏目之间切换保留头像、姓名、GitHub／语雀／关于我整行的移动过渡。临时覆盖层不可交互，完成或中断后必须清理；离屏身份区不生成无意义的过渡副本。
- 每日动态、每日关注、设计收藏、抖音收藏共用日期吸顶与跟随规则。日期固定在栏目导航下方，内容滚动到下一日期时更新，沿用各栏目已有的日期格式和排序。
- **日期左、筛选右。** 不把整组推向右侧。手机日期与筛选均用 0.74rem / 500，日期降调为辅助灰；导航比日期略强，内容标题保持阅读主角。
- 每日动态使用站内 Radix 单选菜单，包含全部、精选和已有分类；右侧对齐展开，当前项有勾选。菜单细线边框、黑白灰、无阴影，选项至少 44px 高；不用浏览器原生 select。
- 筛选按钮保留焦点，空结果仍可切回全部。其他三个日期栏目没有分类功能时，只显示日期，不虚构筛选控件。

### 技术信号场

- 完整词库保留在代码中，常规动效同屏约显示 12 个节点；词条以固定种子选取，保证 SSR 与 hydration 产出同一份词库。
- 动效形态是从右向左的弹幕：6 条泳道各自只运行一个连续轨道，轨道以两份等宽序列无缝循环并用大间距维持稀疏；词条经过容器两侧时被渐隐遮罩裁切——裁切是弹幕语义的一部分（流动的信号，而非陈列的标签），是已裁决的有意设计，不要当作溢出缺陷修复。
- `prefers-reduced-motion` 的静态终态是 3×4 规则网格：这是登记在册的例外——静态形态优先完整可读，不复刻弹幕；两种形态不要求布局同构。

- 持续滚动提供 44px 暂停／恢复按钮；减少动态效果时使用静态词条并隐藏无用的暂停按钮。

### 双语简介

- 桌面首访简介在 Loading 后播放一次：英文标题与正文逐字显示 → 英文正文与标题逐字删除 → 空标题光标闪烁两次 → 中文标题与正文逐字显示。正文稳定为中文后，标题以多语言问候语持续轮换。
- 删除速率快于输入；最终可访问、可复制的正文必须是中文；持续变换的多语言只用于标题问候语。
- 打字机序列不提供跳过或快进：它与 Loading 同属首访仪式，仪式成本只对每个会话的首次到访发生，这是已裁决的设计决定。
- 手机直接呈现完整中文介绍，采用 mobile-bio 字号与 1.65 行高，不播放桌面逐字序列。正文宽度跟随身份区，不保留人为最大宽度造成的右侧空白。

### 关于我打印稿

- 身份区 GitHub、语雀同一链接行内有一个"关于我"触发器（打印机图标 + 文字按钮），点击后打开全屏弹层（`role="dialog"`、焦点受控）：一台拟物打印机把小票从出纸口**逐段送出**，`clip-path` 配合 `steps()` 制造机械式走纸节奏；**每一行文字在走纸到达时瞬时以全浓度切出**，不做淡入，也没有压线等覆盖物。
- **受控拟物例外**：打印机的渐变机身、噪声纹理与多重阴影是全站唯一登记的拟物/阴影语言——它模拟真实打印的触觉仪式，纯平面规则只在这一台机器上让位；机器之外的任何界面元素不得援引此例外。
- 小票内容为两列网格：时间段+公司贴左列，描述贴右缘，两端对齐；附注行（如 OPT 一人团队）归属描述列并随右缘对齐。
- 小票是白色纸张（`#f8f8f8`），深色模式下也保持纸色——打印件不随界面换主题；顶部虚线撕边、底部 SVG 锯齿边，内容为等宽字体（打印语文义，不属于技术信号场的等宽规则）。
- 打印状态文案在机器屏幕上（"正在打印个人经历…" → "打印完成 · 请取走小票"）；经关闭按钮、背板点击或 Esc 收卷（快速反向动画）。
- `prefers-reduced-motion` 下小票直接完整出现，无走纸动画。

### 每日关注流

- 这是**判断流**：标题是他的判断式题名，策展解析是正文主角（`0.82rem`、`#3f3f42`），不是次要灰字——整条流读作他的批注，而不是他转发的列表。
- 每日关注按来源拆分：`/curation` 只呈现 X 来源，抖音来源条目由「抖音收藏」（`/douyin`）单独成流；两条流共用同一套剪报簿样张与密度，只是登记栏的来源行分别为 `X · @handle` 与 `抖音 · 作者名`。
- 每条内容是一行登记簿记录，不是卡片：左列是固定宽（`minmax(7rem,8.5rem)`）的 quiet 登记栏（日期、来源与作者、附件逐行，`0.72rem`；附件是从 media/quoteContext 归并的登记词——图片/视频/GIF/引用，有才出现），右列是标题与解析；不使用方向箭头，整行即是链接。
- 剪报簿结构：解析之下随附**来源摘录**——X 条目剥离 t.co 短链，抖音条目采用经审核的转写/OCR 证据摘录；统一折叠空白、两行裁切（`-webkit-line-clamp:2`），`1px` 左引线（`#dededf`）+ 降调灰（`#656568`、`0.78rem`）标记"来源声音"，与解析的主角层级区分；再下一行是 quiet 标签登记行（`0.67rem`、`·` 分隔，纯文本不用徽章）。
- 行与行之间只用 `1px` 分隔线；hover 只让标题转向墨色，禁止增加背景卡、阴影或彩色条。
- 标题、摘要和链接保留真实来源与日期；行密度由上下 `1.5rem` 内边距维持，三条内容流共用同一密度与栏宽。
- 窄屏（≤900px）收敛为单列：登记栏回到标题上方，以 `·` 分隔为一行。

### 设计收藏流

- `/design` 是每日关注的高置信设计相关子集，不复制第二份内容；同步阶段以原文、外链与图片/视频代表帧判断相关性，只有 `status=include` 的 X 条目进入公开列表。
- 继续使用判断流的双列登记簿与细线分隔；左列依次登记收录日期、`X · @handle` 与设计子类（多个子类以 `·` 分隔），右列保持题名、摘要、来源摘录和标签的原有层级。
- 有可用 X `videoUrl` 时在右侧正文列内直接显示原生控制条播放器，经 `/api/x-media` 代理播放；播放器与详情标题链接分离，禁止把交互式视频嵌套进整行链接。加载失败在图注内保留 quiet 状态文案与「在 X 上查看原视频」出口；无视频时保持普通判断流，不增加空媒体占位。
- animated GIF 仅在常规动态偏好下自动播放并循环；`prefers-reduced-motion` 下必须关闭两者，保留封面与原生手动播放控件。普通视频不自动播放。
- 空列表只在流尾居中显示 quiet 文案「暂时没有高置信度的设计收藏。」，不增加插画、卡片、行动按钮或空占位骨架。

### 策展详情

- 保持同一身份轨；页面是**摊开的剪报簿对页**（`curation-detail--spread`）：头部通栏（收录日期 quiet 行 → detail headline → 摘要 → 标签 pill 行），不再使用 eyebrow 窄列网格。
- 主体 `curation-detail__body` 为 `minmax(0,2fr) / minmax(0,3fr)` 对页双列：左页是**原推剪报**样张贴片，右页是**深度解析**；两列的 eyebrow 标签同基线对齐，是页面上仅剩的"栏题"语言。
- 左页样张在桌面（≥901px）sticky 钉住（`top:2rem`、`max-height:calc(100dvh - 4rem)`、超高时列内 thin 滚动条滚动），解析滚动时证据始终在视线内；窄屏单列堆叠，顺序为头部 → 剪报 → 解析 → 来源 → 相邻。
- **来源摘录**样张贴片：细线框（`1px var(--line)`、`.75rem` 圆角）把证据"贴"在页面上——片内署名行保留平台、作者与发布日期；X 原文继续展开 t.co 链接、引用与媒体，抖音展示经审核的转写/OCR 摘录；媒体只认 `12px` 圆角与 hover 缩放，不另加阴影。
- 原始来源通栏（eyebrow 在上、细线链接列表）；**相邻剪报**收束阅读：页底双列登记行（上一则·较新收录 / 下一则·较早收录，quiet 标签 + stream headline 题名，第二条右对齐）；单侧存在时"下一则"仍落右列（`data-dir` + `:only-child`），窄屏单列复位；排序与列表完全一致，且相邻导航不跨来源——抖音条目只在抖音条目间翻页，X 条目只在 X 条目间翻页。详情页返回链接同样按来源分流：抖音条目返回「抖音收藏」，X 条目返回「每日关注」。
- Markdown 内容可以有阅读型排版、代码块与表格，但不得回退到旧版工作台/知识库壳层；`works` / `open-source` 详情继续复用旧两列骨架，`--spread` 修饰类是两者之间的隔离边界。

### Markdown 正文（全站统一）

- 所有长文 Markdown（策展解析、开源中文阅读版等）只用全局 `.article-markdown` 一套排版：正文 `.92rem`/`1.78`，标题 610/`-0.026em`（h1 `1.4rem` → h4 `.92rem`），代码块深色底、表格 `.82rem`。
- 问答等对话场景的 Markdown 可以用更密的正文（body 范围内），但标题字重/字距与 `.article-markdown` 同档。
- 新增 Markdown 场景不得再建第三套排版；模块级 Markdown 样式一律视为 drift。

### 每日动态流

- 这是**跟踪流**：新闻题名保持主角，摘要为 quiet 文摘——与判断流（每日关注、开源关注）的层级差异是有意的，判断流读他的文字，跟踪流读事件本身。
- 与每日关注流同构：按日分组（桌面日期用 Title 档，手机日期按辅助工具栏档；星期/条数为 quiet），条目是 1px 分隔线的连续流，不做时间轴轨道、圆点或卡片。
- 条目结构与每日关注流同为登记簿：左列登记栏为时刻、出处（列表只显示出处名，@handle 留在详情页）、分类（「精选」只是登记栏里的一行 quiet 文本，不做徽章），右列 stream headline 标题与摘要；无方向箭头，不展示上游评分。
- 窄屏元信息以 `·` 分隔为一行，分隔点尾随在前一项之后，折行时行首不出现孤点。
- 滚动分页加载，追加条目以 `isAppended` 标记播放 0.45rem 上浮 + 32ms 阶梯的渐入；分类筛选切换时整列重挂载，过滤后前 8 行重播同一阶梯（`FILTER_REVEAL_COUNT`），分类当前态由菜单勾选与触发器文字表达，不再使用滑动指示线；空态、加载骨架、失败重试齐备。

### 每日动态详情

- 与策展详情共用身份轨与桌面滚动容器；顶栏只有返回与主题按钮（桌面隐藏顶栏主题按钮，避免与身份轨重复）。
- 结构：kicker（分类 · 精选，quiet 文本）→ detail headline 级 h1 → 元信息 → 导读 → 推荐理由（仅精选条目有数据，左侧 1px 引线）→ 实心 pill 原文出口（external-link 组件）+ quiet 域名。

### 开源关注流

- 与每日关注流同为**判断流**：左列登记栏为主题分类与状态（持续跟踪/已提炼/计划试用等），右列为仓库名（stream headline 档）、他的中文判读（正文主角，`0.82rem`、`#3f3f42`），以及一行 quiet 索引文本（维度 · 类型，以 `·` 分隔）。
- 不使用带边框的 tag 徽章，不使用方向箭头；行分隔线、hover 行为与窄屏收敛与另两条流一致。
- 详情页与每日动态详情同构：返回 → 仓库名 h1 → 判读（`personalNote`，判断流正文主角档 `0.82rem`、`#3f3f42`）→ 仓库文档 tabs → 实心 pill「在 GitHub 查看仓库」+ quiet 域名。判读必须在详情页出现，不能只在列表里当钩子；GitHub 出口必须有，访客读完判读的最高点不能没有顺势出口。

### 构建版块

- `/works` 是稳定策展而非每日流，刊名统一为「构建」：项目是能力、实验、决策、实践与里程碑的唯一根上下文，不建立脱离项目的全局实验池。网站只读取 `data/curation.sqlite` 中审核通过的项目级公开快照；本机 Git、文档、Codex 证据、草稿和检查点不进入运行时读取面。
- 列表是「案头卷宗」与登记行的双形态，由公开快照的 `shots` 决定：有真实样张的项目摊开成卷宗块，无样张的项目保持紧凑登记行。两种形态都在定位之后展示「当前关注」与最近三条项目记录，记录只用细线、类型、题名与状态，不做卡片或彩色徽章。
- 卷宗块三段式：题名（`1.4rem` / 610 / `-0.026em`，限宽 `40rem`）与一句话定位（`.82rem` / `#3f3f42`，限宽 `38rem`）居上；真实页面截图横排成样张带（桌面高 `clamp(11rem, 22vw, 13.5rem)`、窄屏宽 `66vw`，横向滚动 + thin 滚动条，1px 细线描边 + `.75rem` 媒体圆角，hover `scale(1.015)`，图注 `.72rem` quiet）；底栏左侧为时间·状态·角色登记行与技术栈 quiet 行（`·` 分隔），右侧为构建笔记 / 在线访问 / GitHub 出口（细竖线分隔，hover 右移；`url` / `repo` 来自公开项目快照，有才渲染）。
- 样张点击开灯箱看大图：原生 `<dialog>` + `showModal()`，`::backdrop` 为 `rgb(0 0 0 / 72%)`，图限 `92vw` / `84dvh`，图注居中并带 `n / N` 计数，关闭按钮为右上角圆形深色底 X；Esc、背板点击、关闭按钮三条退出路径，开启时锁页面滚动，触发按钮有 `zoom-in` 光标与可见 focus 环。同一作品的样张在灯箱内可切换：左右两侧圆形箭头按钮与键盘 ←/→，循环取模；多于一张时才渲染切换件。切换有方向感：旧图脱出布局反向滑出、新图沿切换方向横移滑入（240ms），从样张带点开则是缩放就位；`prefers-reduced-motion` 下直接换图。灯箱图源与样张同源（2400w JPEG，由 `next/image` 按 `sizes` 分发）。
- 样张是作品页唯一的媒体主角：必须是真实页面截图，不用示意图或插画替代；沿用全站 1px 细线 + 留白，无卡片无阴影。
- 登记行与开源关注同构：左列元信息（时间段 · 状态 · 角色），右列标题 + 一句话定位 + 当前关注 + 最近记录 + 技术栈 quiet 行。
- 条目少，卷宗块与登记行共存，仍靠留白而非容器撑份量。
- 详情复用策展详情骨架（`curation-detail`）：元信息 + 技术栈 pill、标题/定位之后是一行项目内文字索引（正在验证 / 当前能力 / 实验记录 / 关键决策 / 沉淀实践 / 项目里程碑），再以登记簿行展开记录。左列是日期与状态，右列是题名、结论、可选 Markdown 与所属证据；证据链接必须跟随记录，不单独堆成资料仓库。
- `资料截至` 表示公开内容覆盖的最近证据时间，SQLite `published_at` 表示当前项目行的最近发布时间；采集与待审阶段不改变公开读取面，显式发布则按项目 upsert 后回读验证。重复发布同一 `revision` 仍会刷新 `published_at`。
- 数据流图等技术图示用代码块内的等宽 ASCII，不引入彩色插图。

### 问一问

- 对话页只有三个动词：问、等、读。组合器（shadcn `InputGroup`）跟随对话流并包含底部安全区：内容不足一屏时紧跟内容下方，超出一屏时消息区内部滚动、组合器停靠可视区底部，不滚走。
- 桌面端刊头、消息列与组合器共享同一根居中的 `42rem` 栏：三者左右缘完全对齐，宽屏下的留白均分到两侧，刊头不再通栏。
- 检索范围在组合器底部左侧（`DropdownMenu`）：触发器显示当前范围，点击向上展开单选列表（全部/关于我/构建/每日动态/每日关注/开源关注）；不使用平铺的分段控件。
- 用户消息是右侧墨泡（短文本）；助手回答是全宽阅读面（长文 Markdown），不是气泡；说话人由对齐方向表达，铭牌只做 sr-only。
- 空状态的建议问题是首屏产品：只保留底部分隔线，不出现整圈按钮边框；通栏细线行、移动端 2.75rem 触达；引用来源是细线列表，保留焦点环。
- 回答完成后给出追问建议：复用建议行的细线语言，只展示本次会话还没用过的建议问题（至多 2 条），点击只填入组合器并聚焦，不代为发送。
- 移动端触达目标 ≥2.75rem（发送、范围、建议、引用），输入字号 `1rem` 避免 iOS 聚焦缩放。

### Loading 与主题

- Loading 是根布局的一次性全屏遮罩：电池绿色 + 插画序列，完成后向上揭幕。
- 揭幕（上滑）开始时内容流同步播放「档案摊开」入场：刊头与首批至多 6 个内容单元按 32ms 阶梯做 0.45rem 上浮 + 淡入，总时长约 0.5s，可中断、终态交还 CSS；它与仪式共用同一份 sessionStorage 成本模型——每会话仅一次，回访与切版块都不重播，`prefers-reduced-motion` 下不武装、内容直接呈现；揭幕事件未到达时有兜底恢复，页面绝不留在隐藏态。
- 首访仪式是固定时长的刻意设计：常规模式下 5 秒电池充电序列，不与真实加载进度挂钩，也不提供跳过；`prefers-reduced-motion` 下约 160ms 直接揭幕。每个会话仅播放一次（sessionStorage），回访零成本——这个成本模型本身是已裁决的设计决定，不要再按真实就绪事件改造它。
- 主题按钮为圆形图标控制，深色使用轻边框与深色底。深色主题只交换黑白灰令牌，不创建另一套视觉语言。
- 长列表标题即时更新主题颜色，不对屏外标题做延迟颜色过渡，避免进入视口时出现低对比度闪变。
- 动效分别服务首访揭幕、身份展示、导航定位、追加反馈和媒体状态。所有动效必须可清理、有稳定终态，并提供减少动态效果路径。

## Do's and Don'ts

### Do:

- **Do** 以 `.curation-home` 的“身份轨 + 内容流”作为新页面的默认骨架。
- **Do** 在内容区使用连续分隔线、语义标题与真实元信息表达层级。
- **Do** 将新的技术词、简介内容与策展条目放进既有组件和数据模型，而不是另建展示壳。
- **Do** 为新的动效补齐 `prefers-reduced-motion` 的静态或即时状态。
- **Do** 先在桌面宽度验证两栏节奏、词节点不重叠、正文不留无意义右侧空白。

### Don't:

- **Don't** 恢复知识库、语雀同步、工作台表格或旧 `WorkspaceFrame` 视觉结构。
- **Don't** 用卡片网格、玻璃拟态、大阴影或高饱和装饰色承载策展内容。
- **Don't** 将 Loading 的绿色或此前试验的黄色扩散到加载完成后的页面。
- **Don't** 为技术感滥用等宽字体；等宽字体只属于技术信号场与代码语义。
- **Don't** 让技术节点、正文文本或详情标题溢出、堆叠或破坏两栏阅读线。
