// 复制本文件为 Secrets.xcconfig 并填入真实值；Secrets.xcconfig 已被 gitignore。
// 注意：xcconfig 里 // 会被当成注释，URL 必须用 $() 空变量拼接。
//
// App 只携带 publishable key；service-role、KIMI key 等绝不进这里。

SUPABASE_URL = https:/$()/your-project.supabase.co
SUPABASE_PUBLISHABLE_KEY =
SITE_BASE_URL = http:/$()/127.0.0.1:3000
